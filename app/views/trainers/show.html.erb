<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<% if @trainer.id == current_trainer.id %>
   <%= button_to "Create New Pokemon!", "/pokemons/new", :class => "button medium", :method => :get %>
<% end  %>
  
<table class="table">
  <tbody>
    <%= @trainer.pokemons.each do |pokemon| %>
      <tr>
        <td>Pokemon: <%= pokemon.name %>, Level: <%= pokemon.level %>, Health: <%= pokemon.health %> </td>
        <td> <%= button_to "Damage!", damage_path(id: pokemon), :class => "button medium", :method => :patch %> </td>
      </tr>
    <% end %>
  </tbody>
</table>
